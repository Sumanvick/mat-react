# Building Frontend for Roo-m-eeting in Node and React

Roomeeting ui part

## Requirements

- Node and npm

## Installation

- Clone the repo: `.git`
- Install dependencies: `npm install`
- Bundule Project: `webpack`
- Start the server: `npm start`
- Default Port: `5050`